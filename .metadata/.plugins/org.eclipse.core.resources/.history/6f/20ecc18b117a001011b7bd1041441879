<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org" lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>AimUp - Início</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" rel="stylesheet" />
  <link rel="shortcut icon" href="/img/LogoAimUp.png" type="image/x-icon" />
  <style>
    body{background:linear-gradient(0deg,#6fbb96 0%,#23a86b 100%);font-family:'Segoe UI',sans-serif;color:#fff;min-height:100vh}
    .card{background-color:rgba(255,255,255,.12);border:none;border-radius:20px;backdrop-filter:blur(5px);color:#fff}
  </style>
</head>
<body>
  <div class="container-fluid p-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
      <!-- corrigido: agora navega para /grupos -->
      <a class="text-white" th:href="@{/grupos}"><i class="fas fa-bars"></i></a>

      <h5 class="fw-bold text-white mb-0">Bem-vindo(a), <span th:text="${usuario?.nome}">Usuário</span></h5>
      <a href="/perfil"><i class="fas fa-user-circle fa-2x text-white"></i></a>
    </div>

    <div class="row g-4">
      <!-- MEUS GRUPOS -->
      <div class="col-lg-8">
        <div class="card p-4">
          <div class="d-flex justify-content-between align-items-center mb-3">
            <h4 class="fw-bold mb-0">Meus Grupos</h4>
            <!-- <a class="btn btn-light text-success" href="/grupos/criar-form"><i class="fa fa-plus"></i> Criar grupo</a> -->
          </div>

          <div th:if="${#lists.isEmpty(grupos)}" class="alert alert-light text-dark">
            Você ainda não participa de nenhum grupo. Quando um admin te adicionar (ou você criar), ele aparecerá aqui.
          </div>

          <ul class="list-group" th:if="${!#lists.isEmpty(grupos)}">
            <li class="list-group-item d-flex justify-content-between align-items-center"
                th:each="g : ${grupos}">
              <div>
                <strong th:text="${g.nome}">Nome do Grupo</strong><br/>
                <small class="text-muted">ID: <span th:text="${g.id}">1</span></small>
              </div>
              <div class="d-flex gap-2">
                <a class="btn btn-outline-secondary" th:href="@{|/grupos/${g.id}|}">Detalhes</a>
                <a class="btn btn-success" th:href="@{|/tarefas/grupo/${g.id}|}">Ver Tarefas</a>
              </div>
            </li>
          </ul>
        </div>
      </div>

      <!-- LEMBRETES -->
      <div class="col-lg-4">
        <div class="card p-4 bg-white text-dark">
          <h5 class="fw-bold text-success">Lembretes</h5>
          <div th:if="${#lists.isEmpty(lembretes)}" class="text-muted">Sem lembretes por enquanto.</div>
          <ul class="list-unstyled" th:if="${!#lists.isEmpty(lembretes)}">
            <li th:each="l : ${lembretes}" class="mb-2">
              <strong th:text="${l.titulo}">Título</strong><br/>
              <small class="text-muted" th:text="${l.descricao}">Descrição</small>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>
</body>
</html>

<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org" lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Chat do Grupo</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body{background:linear-gradient(0deg,#6fbb96 0%,#23a86b 100%);font-family:'Segoe UI',sans-serif;color:#fff;min-height:100vh}
    .card{background-color:rgba(255,255,255,.12);border:none;border-radius:20px;backdrop-filter:blur(5px);color:#fff}
    .chat-box{height:70vh;overflow-y:auto}
    .avatar{width:36px;height:36px;border-radius:50%;object-fit:cover;border:2px solid #fff}
    .bubble{max-width:70%;border-radius:16px;padding:10px 12px}
    .mine   {background:#e8f7ee;color:#0f5132}   /* você (claro) */
    .other  {background:#2b2f31;color:#fff}      /* outros (escuro) */
    .name{font-size:12px;font-weight:600;opacity:.9;margin-bottom:2px}
    .time{font-size:11px;opacity:.7}
  </style>
</head>
<body>
<div class="container py-4">
  <div class="d-flex justify-content-between align-items-center">
    <a class="btn btn-outline-light" th:href="/home"><i class="fa fa-arrow-left"></i> Voltar</a>
    <h3 class="fw-bold mb-0" th:text="${grupo.nome}">Grupo</h3>
    <span class="badge bg-light text-dark" th:text="${qtdMembros + ' membros'}">0 membros</span>
  </div>

  <!-- mensagens -->
  <div id="chatBox" class="card p-3 mt-3 chat-box bg-white text-dark">
    <div th:if="${#lists.isEmpty(mensagens)}" class="text-muted">Sem mensagens ainda.</div>

    <div th:each="m : ${mensagens}">
      <!-- Se for SUA mensagem, à direita -->
      <div class="d-flex mb-2" th:if="${m.autor.id == usuario.id}" style="justify-content: end;">
        <div class="me-2 text-end">
          <div class="bubble mine">
            <div class="name" th:text="${m.autor.nome}">Você</div>
            <div th:text="${m.conteudo}">conteúdo</div>
            <div class="time mt-1" th:text="${#temporals.format(m.criadaEm,'HH:mm')}">00:00</div>
          </div>
        </div>
        <img class="avatar"
             th:src="${(m.autor.fotoPerfil != null && !#strings.isEmpty(m.autor.fotoPerfil)) ? m.autor.fotoPerfil : '/img/LogoAimUp.png'}"
             alt="avatar">
      </div>

      <!-- Se for mensagem de OUTRO, à esquerda -->
      <div class="d-flex mb-2" th:if="${m.autor.id != usuario.id}">
        <img class="avatar me-2"
             th:src="${(m.autor.fotoPerfil != null && !#strings.isEmpty(m.autor.fotoPerfil)) ? m.autor.fotoPerfil : '/img/LogoAimUp.png'}"
             alt="avatar">
        <div>
          <div class="bubble other">
            <div class="name" th:text="${m.autor.nome}">Nome</div>
            <div th:text="${m.conteudo}">conteúdo</div>
            <div class="time mt-1" th:text="${#temporals.format(m.criadaEm,'HH:mm')}">00:00</div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- enviar -->
  <form class="input-group mt-3" th:action="@{|/grupos/${grupo.id}/chat/enviar|}" method="post">
    <input type="hidden" th:name="${_csrf.parameterName}" th:value="${_csrf.token}" />
    <input id="chatInput" name="mensagem" type="text" class="form-control" placeholder="Mensagem..." required>
    <button class="btn btn-success" type="submit">Enviar</button>
  </form>
</div>

<script>
  // rolar para o final ao carregar
  (function(){
    const box = document.getElementById('chatBox');
    if (box) box.scrollTop = box.scrollHeight;
  })();
</script>
</body>
</html>

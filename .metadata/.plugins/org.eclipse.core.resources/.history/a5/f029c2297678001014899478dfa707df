<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org" lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Login</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>
<h1>Entrar</h1>

<!-- mensagens opcionais -->
<div th:if="${param.registered}">Cadastro realizado! Faça login.</div>
<div th:if="${param.logout}">Você saiu da sessão.</div>
<div th:if="${param.error}">Credenciais inválidas.</div>

<form th:action="@{/login}" method="post">
  <!-- CSRF obrigatório -->
  <input type="hidden" th:name="${_csrf.parameterName}" th:value="${_csrf.token}"/>

  <!-- email é o "username" para o Spring Security -->
  <label for="email">E-mail</label>
  <input id="email" type="email" name="email" placeholder="seu@email.com" required/>

  <label for="password">Senha</label>
  <input id="password" type="password" name="password" placeholder="••••••••" required/>

  <button type="submit">Entrar</button>
</form>

<p>Não tem conta? <a th:href="@{/cadastro}">Cadastre-se</a></p>
</body>
</html>

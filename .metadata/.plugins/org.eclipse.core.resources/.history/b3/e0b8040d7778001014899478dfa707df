<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org" lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Login</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- estilo básico opcional (pode remover se já tiver CSS) -->
  <style>
    body { font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif; margin: 2rem; }
    form { max-width: 420px; display: grid; gap: 12px; }
    label { font-weight: 600; }
    input[type="email"], input[type="password"] {
      width: 100%; padding: 10px; border: 1px solid #cbd5e1; border-radius: 8px;
    }
    .alert { padding: 10px 12px; border-radius: 8px; margin-bottom: 12px; }
    .alert-ok { background: #f0f9ff; color: #075985; border: 1px solid #bae6fd; }
    .alert-warn { background: #fef3c7; color: #92400e; border: 1px solid #fde68a; }
    .alert-error { background: #fef2f2; color: #991b1b; border: 1px solid #fecaca; }
    .actions { margin-top: 8px; display: flex; gap: 8px; align-items: center; }
    button { padding: 10px 14px; border: 0; border-radius: 8px; background: #111827; color: #fff; cursor: pointer; }
    .muted { color: #6b7280; font-size: .95rem; }
    .password-row { display: grid; grid-template-columns: 1fr auto; gap: 8px; align-items: center; }
    .toggle { background: #e5e7eb; color: #111827; padding: 10px 12px; border-radius: 8px; cursor: pointer; }
  </style>
</head>
<body>
  <h1>Entrar</h1>

  <!-- mensagens opcionais -->
  <div th:if="${param.registered}" class="alert alert-ok">Cadastro realizado! Faça login.</div>
  <div th:if="${param.logout}" class="alert alert-warn">Você saiu da sessão.</div>
  <div th:if="${param.error}" class="alert alert-error">Credenciais inválidas.</div>

  <form th:action="@{/login}" method="post" novalidate>
    <!-- CSRF obrigatório -->
    <input type="hidden" th:name="${_csrf.parameterName}" th:value="${_csrf.token}"/>

    <!-- email é o "username" para o Spring Security (configurado como usernameParameter("email")) -->
    <div>
      <label for="email">E-mail</label>
      <input id="email" type="email" name="email" placeholder="seu@email.com"
             autocomplete="username" required autofocus/>
    </div>

    <div>
      <label for="password">Senha</label>
      <div class="password-row">
        <input id="password" type="password" name="password" placeholder="••••••••"
               autocomplete="current-password" required/>
        <button type="button" class="toggle" onclick="
          const p=document.getElementById('password');
          p.type = p.type==='password' ? 'text' : 'password';
        ">mostrar</button>
      </div>
    </div>

    <div class="actions">
      <button type="submit">Entrar</button>
      <span class="muted">Não tem conta? <a th:href="@{/cadastro}">Cadastre-se</a></span>
    </div>
  </form>
</body>
</html>

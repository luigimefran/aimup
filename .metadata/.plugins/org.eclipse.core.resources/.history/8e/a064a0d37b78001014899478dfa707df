<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org" lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Cadastro</title>

  <!-- Bootstrap -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"/>

  <!-- Favicon -->
  <link rel="shortcut icon" href="/img/LogoAimUp.png" type="image/x-icon" />

  <!-- Mesmo estilo do login -->
  <style>
    body {
      background: linear-gradient(0deg, #6fbb96 0%, #23a86b 100%);
      min-height: 100vh;
      font-family: 'Poppins', system-ui, -apple-system, Segoe UI, Roboto, sans-serif;
      color: #fff;
      margin: 0;
      display: grid;
      place-items: center;
      padding: 24px;
    }
    ::placeholder { color: #ffffff !important; opacity: 1; }

    .login-box {
      background: rgba(255,255,255,0.1);
      border-radius: 16px;
      padding: 2rem;
      backdrop-filter: blur(6px);
      box-shadow: 0 10px 30px rgba(0,0,0,0.15);
      width: 100%;
      max-width: 420px;
    }

    .brand {
      display: grid;
      place-items: center;
      margin-bottom: 1rem;
    }

    .form-control {
      background: transparent !important;
      color: #fff !important;
      border: 2px solid #ffffff !important;
      border-radius: 20px;
      padding: 10px 14px;
    }
    .form-control:focus {
      box-shadow: 0 0 0 .2rem rgba(255,255,255,.25);
    }

    .btn-aimup {
      background: #fff;
      color: #23a86b;
      border: 2px solid #fff;
      border-radius: 20px;
      padding: 10px 14px;
      font-weight: 600;
      transition: transform .08s ease-in-out;
    }
    .btn-aimup:hover { transform: scale(1.02); }

    a { color: #ffffff; text-decoration: underline; }
    a:hover { color: #e9fff4; }
  </style>
</head>

<body>
  <div class="login-box">
    <div class="brand">
      <img src="/img/Logo AimUp.png" alt="Logo AimUp" style="width: 220px; height: auto;" />
    </div>

    <h2 class="text-center mb-3">Criar conta</h2>

    <!-- erro global do controller (ex.: e-mail já existe) -->
    <div th:if="${erro}" th:text="${erro}" class="alert alert-danger py-2"></div>

    <!-- resumo de erros Bean Validation -->
    <div th:if="${#fields.hasAnyErrors()}" class="alert alert-danger py-2">
      Corrija os campos destacados abaixo.
    </div>

    <form th:action="@{/cadastro}" th:object="${usuario}" method="post" novalidate>
      <!-- CSRF -->
      <input type="hidden" th:name="${_csrf.parameterName}" th:value="${_csrf.token}"/>

      <div class="mb-3">
        <label for="nome" class="form-label">Nome</label>
        <input id="nome" type="text" th:field="*{nome}" name="nome" class="form-control"
               placeholder="Seu nome" autocomplete="name" required
               th:classappend="${#fields.hasErrors('nome')} ? 'is-invalid'"/>
        <div th:if="${#fields.hasErrors('nome')}" th:errors="*{nome}" class="invalid-feedback d-block"></div>
      </div>

      <div class="mb-3">
        <label for="email" class="form-label">E-mail</label>
        <input id="email" type="email" th:field="*{email}" name="email" class="form-control"
               placeholder="seu@email.com" autocomplete="email" required
               th:classappend="${#fields.hasErrors('email')} ? 'is-invalid'"/>
        <div th:if="${#fields.hasErrors('email')}" th:errors="*{email}" class="invalid-feedback d-block"></div>
      </div>

      <div class="mb-3">
        <label for="senha" class="form-label">Senha</label>
        <!-- IMPORTANTE: o atributo da entidade deve ser 'senha' -->
        <input id="senha" type="password" th:field="*{senha}" name="senha" class="form-control"
               placeholder="Crie uma senha" autocomplete="new-password" required
               th:classappend="${#fields.hasErrors('senha')} ? 'is-invalid'"/>
        <div th:if="${#fields.hasErrors('senha')}" th:errors="*{senha}" class="invalid-feedback d-block"></div>
      </div>

      <button type="submit" class="btn btn-aimup w-100">Cadastrar</button>
    </form>

    <div class="text-center mt-3">
      Já tem conta? <a th:href="@{/login}">Entrar</a>
    </div>
  </div>
</body>
</html>
